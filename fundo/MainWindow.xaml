<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="fundo.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:fundo"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="fundo">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Padding="20" RowSpacing="15">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Dateiname -->
        <StackPanel Spacing="5">
            <TextBlock Text="Dateiname" FontWeight="SemiBold"/>
            <TextBox x:Name="FileNameTextBox"
                 PlaceholderText="z.B. *.txt oder bericht"/>
        </StackPanel>

        <!-- Verzeichnis -->
        <StackPanel Grid.Row="1" Spacing="5">
            <TextBlock Text="Suchverzeichnis" FontWeight="SemiBold"/>
            <TextBox x:Name="DirectoryTextBox"
                 PlaceholderText="z.B. C:\Dokumente"/>
        </StackPanel>

        <!-- NavigationView für Filter -->
        <NavigationView Grid.Row="2"
                    x:Name="FilterNavigationView"
                    PaneDisplayMode="Left"
                    IsSettingsVisible="False"
                    SelectionChanged="FilterNavigationView_SelectionChanged">

            <NavigationView.MenuItems>
                <NavigationViewItem Content="Datum" Tag="date" Icon="Calendar"/>
                <NavigationViewItem Content="Dateigröße" Tag="size" Icon="Page"/>
                <NavigationViewItem Content="Dateiflags" Tag="attributes" Icon="Tag"/>
                <NavigationViewItem Content="Dateierweiterungen" Tag="extensions" Icon="Document"/>
            </NavigationView.MenuItems>

            <Frame x:Name="ContentFrame"/>
        </NavigationView>

        <!-- Suchbutton -->
        <Button Grid.Row="3"
            Content="Suche starten"
            HorizontalAlignment="Right"
            Padding="20,8"/>
    </Grid>

</Window>
