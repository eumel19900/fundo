<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="fundo.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:fundo"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:src="using:fundo.gui.control" xmlns:model="using:fundo.core.Search"
    xmlns:job="using:fundo.gui.Job"
    mc:Ignorable="d"
    Title="Fundo">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <NavigationView Grid.Row="0"
             x:Name="FilterNavigationView"
             PaneDisplayMode="LeftMinimal"
             IsPaneOpen="False"
             IsSettingsVisible="True"
             IsBackButtonVisible="Collapsed"
             SelectionChanged="FilterNavigationView_SelectionChanged">

            <NavigationView.MenuItems>
                <NavigationViewItem x:Name="SearchPageNav" Content="Search" Tag="search" IsSelected="True">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Find" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem x:Name="AboutPageNav" Content="About" Tag="about">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Help" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <Frame x:Name="MainWindowContentFrame" Margin="32,0,0,0"/>
        </NavigationView>

        <!-- Job status bar for background operations --><!--
        <job:JobStatusBar Grid.Row="1" x:Name="MainJobStatusBar"/>-->
    </Grid>

</Window>
