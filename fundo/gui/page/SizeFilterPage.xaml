<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="fundo.gui.SizeFilterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:fundo.gui"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="20" RowSpacing="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <CheckBox x:Name="FilterBySizeCheckbox" Content="Filter by filesize" Grid.Row="0"/>

        <StackPanel Spacing="5" Grid.Row="1" Orientation="Horizontal">
            <!-- x:Name hier von StackPanel auf ComboBox verschoben -->
            <ComboBox x:Name="FileSizeTypeCombobox"
                      PlaceholderText=""
                      Header="How to compare"
                      Width="200"
                      SelectedIndex="2">
                <x:String>Equals</x:String>
                <x:String>Bigger than</x:String>
                <x:String>Smaller than</x:String>
            </ComboBox>

            <NumberBox
                x:Name="FileSizeValueNumberbox"
                Header="Filesize in KiB"
                Value="10000"
                SpinButtonPlacementMode="Inline"
                SmallChange="10"
                LargeChange="100"
                Width="200"/>
        </StackPanel>
    </Grid>
</Page>
